const initialState = [
  {
    value: "",
    todos: [],
  },
];

const reducer = (state, type) => {
  switch (type) {
      type TYPING_VALUE;
    default:
      throw new Error("Lỗi reducer");
  }
};
export { initialState };
export default reducer;
