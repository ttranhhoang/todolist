const initialState = [
  {
    value: "",
    todos: [],
  },
];

const reducer = (state, action) => {
  switch (actiong) {
      case TYPING_VALUE:
          return []

    default:
      throw new Error("Lỗi reducer");
  }
};
export { initialState };
export default reducer;
